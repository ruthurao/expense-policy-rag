# Expense Policy RAG

Mini RAG lab: a grounded assistant over one employee expense policy.

Phase 1 is scaffold only. Run instructions land in a later commit.

    cp .env.example .env
    uv sync
    docker compose up -d
